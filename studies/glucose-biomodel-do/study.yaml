schema_version: 4
name: glucose-biomodel-do
title: "Glucose BioModel Digital Objects — Uberon Organ Annotation"
created: '2026-07-26'
status: running
design_status: complete
implementation_status: complete
simulation_status: ran
phase: Evaluate

question: |
  Can glucose-regulation BioModels be annotated with HRA Uberon organ terms
  (via transparent synonym matching) and inverted into an organ->models index?

study_card:
  goal: "Search BioModels for 'glucose regulation', annotate each hit with HRA reference-organ synonyms, and build the organ(Uberon)->model-ids index."
  mechanism: "BiomodelDOCatalogStep.update -> build_biomodel_do_catalog(query, max_results) = search_biomodels_detailed + build_organ_index(HRA reference organs) + annotate_biomodel (synonym-match) -> biomodel_dos + organ_to_models -> RAMEmitter."
  expected_result: "At least one glucose-regulation BioModel maps to at least one HRA organ (pancreas is expected to dominate given insulin/glucagon/beta-cell synonyms)."
  main_expert_question: "Is synonym-based annotation (vs a real ontology/NLP classifier) precise enough to trust the organ->models index for downstream multiscale coupling?"

readouts:
  - "count of biomodel DOs built"
  - "count of distinct organs (Uberon CURIEs) with >=1 mapped model"
  - "per-organ model counts in organ_to_models"

baseline:
  - name: baseline
    composite: viva_human_atlas.composites.biomodel_do_composite.glucose-biomodel-do
    params:
      query: "glucose regulation"
      max_results: 25
