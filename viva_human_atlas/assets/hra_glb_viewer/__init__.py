"""Package marker so `importlib.resources.files("viva_human_atlas.assets.hra_glb_viewer")`
resolves — the `index.html` + `viewer.js` static assets living alongside this
file are read (never imported) by `viva_human_atlas.viewer_pack.materialize_viewer`."""
